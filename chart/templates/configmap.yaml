apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vscode.fullname" . }}
  labels:
    {{- include "vscode.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
